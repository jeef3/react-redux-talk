import styled from 'styled-components';

const BigPageText = styled.p`
  margin: 0;

  color: rgba(0, 0, 0, 0.4);
  font-size: 48px;
  font-weight: 900;

  text-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.4);
`;
BigPageText.displayName = 'BigPageText';

export default BigPageText;
